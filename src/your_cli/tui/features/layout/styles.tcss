/* ── Layout demo — Grid ─────────────────────────────────────── */

#demo-grid {
    layout: grid;
    grid-size: 3 2;
    grid-gutter: 1 2;
    height: 1fr;
    padding: 1;
}

.grid-cell {
    background: $boost;
    border: solid $primary;
    padding: 1 2;
    content-align: center middle;
}
