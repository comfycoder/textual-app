/* ── Progress demo ──────────────────────────────────────────── */

#progress-body {
    padding: 1 4;
    height: 1fr;
    background: $surface;
}

#demo-sparkline {
    height: 8;
    margin-bottom: 1;
}

#demo-loader {
    width: 6;
    height: 1;
}
