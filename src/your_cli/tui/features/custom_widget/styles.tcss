/* ── Custom widgets ─────────────────────────────────────────── */

#custom-body {
    padding: 1 4;
    height: 1fr;
    background: $surface;
}

#metric-row {
    height: auto;
    margin-bottom: 1;
}

#badge-row {
    height: auto;
    align: left middle;
}
